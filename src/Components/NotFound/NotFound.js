import { Typography } from "@material-ui/core";
import React from "react";

const NotFound = () => {
  return (
    <Typography variant="h1" align="center" gutterTop>
      404 not found
    </Typography>
  );
};

export default NotFound;
